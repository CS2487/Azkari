name: azkar_application
description: Azkari app converted to Provider
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State management
  provider: ^6.1.2

  # Local storage
  shared_preferences: ^2.3.2

  # Timezone & notifications
  flutter_timezone: ^1.0.8
  timezone: ^0.9.4
  flutter_local_notifications: ^17.2.3

  # UI utils
  percent_indicator: ^4.2.3

  # Device features
  vibration: ^1.9.0
  fluttertoast: ^8.2.8
  share_plus: ^10.0.2

  # Localization
  flutter_localizations:
    sdk: flutter

  # Material Icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/json/
